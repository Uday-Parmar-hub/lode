# Corpus layer (fetch + archive). Schema/extraction/dashboard deps added when those land.
httpx>=0.28.0
structlog>=24.1.0
tenacity>=8.2.0
pandas>=2.0.0
openpyxl>=3.1.0
pymupdf>=1.24.0        # fitz — PDF text + figure extraction
anthropic>=0.40.0     # extraction (later)
